app:
  name: telegram-loyal
  run: uvicorn main:app --host 0.0.0.0 --port $PORT
  env:
    PORT: $PORT
    BONUS_DATA_FILE: $BONUS_DATA_FILE
    DEFAULT_EXPIRY_DAYS: $DEFAULT_EXPIRY_DAYS
  ports:
    - $PORT
